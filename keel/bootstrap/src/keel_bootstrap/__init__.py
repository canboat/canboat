"""keel - the CANboat PGN database tool.

Validates the YAML PGN database, generates all derived artifacts
(canboat.xml, pgn.h, lookup.h, fieldtype.h), and serves the local editor.
See keel/DESIGN.md.
"""

__version__ = "0.1.0"
