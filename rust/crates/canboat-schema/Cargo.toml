[package]
name        = "canboat-schema"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
authors     = { workspace = true }
repository  = { workspace = true }
rust-version = { workspace = true }
description = "Static, const-friendly type definitions for the canboat NMEA 2000 PGN database. Holds no data of its own — canboat-core's build script emits the generated `&'static` tables."

[dependencies]
